import { Typography } from "@mui/material";

export default function NoSelected() {
  return (
    <Typography variant='h6' align='center' sx={{mt: 4}}>
      Please select a topic and subtopic to start answering.
    </Typography>
  )
}